<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Presentation.ViewModels"
             x:Class="Presentation.Views.EditDebtPage"
             Title="Edit Debt" x:DataType="viewModels:EditDebtViewModel">
    <VerticalStackLayout>
        <Label Text="Edit Debt" FontSize="24" HorizontalOptions="Center" />
        <Entry Placeholder="Creditor" Text="{Binding Creditor}" />
        <Entry Placeholder="Amount" Text="{Binding Amount}" />
        <Button Text="Save" Clicked="OnSaveButtonClicked" />
        <Button Text="Cancel" Clicked="OnCancelButtonClicked" />
        <Button Text="Delete" Clicked="OnDeleteButtonClicked" />
    </VerticalStackLayout>
</ContentPage>